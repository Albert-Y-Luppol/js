import { SwitchComponentItem } from './switch-component-item';

describe('SwitchComponentItem', () => {
  it('should create an instance', () => {
    expect(new SwitchComponentItem()).toBeTruthy();
  });
});
